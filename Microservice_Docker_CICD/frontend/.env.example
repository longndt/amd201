# Base URL of the API Gateway (Ocelot). The frontend talks ONLY to the gateway,
# which routes to the Auth and Product services behind it.
# Local development (docker-compose or dotnet run):
VITE_API_URL=http://localhost:7000
