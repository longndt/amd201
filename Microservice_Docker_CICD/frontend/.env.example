# Base URLs of the two backend microservices.
# For local development (services running via docker-compose or dotnet run):
VITE_AUTH_API_URL=http://localhost:5001
VITE_PRODUCT_API_URL=http://localhost:5002
