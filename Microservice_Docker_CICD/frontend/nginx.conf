server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Single Page App: always fall back to index.html so React Router works
    # on refresh / deep links.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
